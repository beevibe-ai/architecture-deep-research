node_modules/
dist/
.turbo/
.DS_Store
*.log
.env
.env.*
!.env.example
.adr-runs/
.smoke-runs/
web/dist/

# `.adr/principles.{json,md}` is checked in (the team's principles file);
# per-run review artifacts and the regenerated reports / health / stats are not.
.adr/events.jsonl
.adr/reviews/
.adr/drift/
.adr/principles.html
.adr/principles-health.json
.adr/principle-stats.json
.adr/architecture.spec.json

# Build output and local tooling artifacts (regenerated, not source)
studio/out/
.gstack/
crystal-ball/
