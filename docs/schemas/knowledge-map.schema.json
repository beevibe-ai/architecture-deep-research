{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/beevibe-ai/architecture-deep-research/schemas/knowledge-map.schema.json",
  "title": "Architecture Deep Research — Knowledge Map",
  "type": "object",
  "required": [
    "version",
    "acquisition_mode",
    "acquisition_rule",
    "candidates",
    "off_topic_candidates"
  ],
  "properties": {
    "version": { "type": "string" },
    "acquisition_mode": { "type": "string" },
    "acquisition_rule": { "type": "string" },
    "candidates": {
      "type": "array",
      "items": { "$ref": "#/$defs/candidate" }
    },
    "off_topic_candidates": {
      "type": "array",
      "items": { "$ref": "#/$defs/candidate" }
    }
  },
  "$defs": {
    "candidate": {
      "type": "object",
      "required": [
        "name",
        "label",
        "evidence_depth",
        "evidence_count",
        "source_types",
        "citations",
        "support",
        "warnings",
        "rejections",
        "score"
      ],
      "properties": {
        "name": { "type": "string" },
        "label": { "type": "string" },
        "evidence_depth": {
          "type": "string",
          "enum": ["thick", "medium", "thin"]
        },
        "evidence_count": { "type": "integer", "minimum": 0 },
        "source_types": {
          "type": "array",
          "items": { "type": "string" }
        },
        "citations": {
          "type": "array",
          "items": { "type": "integer", "minimum": 1 }
        },
        "support": {
          "type": "array",
          "items": { "$ref": "#/$defs/claim" }
        },
        "warnings": {
          "type": "array",
          "items": { "$ref": "#/$defs/claim" }
        },
        "rejections": {
          "type": "array",
          "items": { "$ref": "#/$defs/claim" }
        },
        "score": { "type": "number", "minimum": 0 }
      },
      "additionalProperties": true
    },
    "claim": {
      "type": "object",
      "required": ["citation_id", "claim", "confidence", "source_type", "url"],
      "properties": {
        "citation_id": { "type": "integer", "minimum": 1 },
        "claim": { "type": "string" },
        "risk_or_fit": { "type": "string" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "source_type": { "type": "string" },
        "url": { "type": "string" },
        "url": { "type": "string" }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
