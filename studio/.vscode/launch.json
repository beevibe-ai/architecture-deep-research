{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ADR Studio (studio folder opened directly)",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "studio: build (local)",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    }
  ]
}
